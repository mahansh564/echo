Place the `whisper-cli` sidecar binary here for packaged builds.

Expected bundle path base: `src-tauri/binaries/whisper-cli`.
Tauri adds platform-specific suffixes during bundling.

Runtime config can also point to a binary on PATH via:
`asr_sidecar_path = "whisper-cli"`
